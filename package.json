{
  "name": "fax-downloader",
  "version": "1.0.1",
  "description": "Download files from URLs with fax-downloader. This works for image downloads and any file that is hosted an a CDN.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "FAXES",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
